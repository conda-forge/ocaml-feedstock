#!/usr/bin/env bash
# OCaml assembler wrapper - expands CONDA_OCAML_AS environment variable
# On macOS, CONDA_OCAML_AS may include '-c' flag (e.g., "clang -c")
. "$(dirname "$0")/conda-ocaml-common"
ocaml_exec_simple "${CONDA_OCAML_AS:-}" as "$@"
