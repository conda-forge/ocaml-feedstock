c_compiler:
  - if: not unix
    then:
      - gcc

c_compiler_version:
  - if: not unix
    then:
      - 14

c_stdlib:
  - if: not unix
    then:
      - m2w64-sysroot

c_stdlib_version:
  - if: not unix
    then:
      - 12
