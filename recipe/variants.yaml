c_compiler:
  - if: not unix
    then:
      - vs2022
      - gcc

cross_target_platform_:
  - linux-64
  - linux-aarch64
  - linux-ppc64le
  - osx-64
  - osx-arm64
  - win-64

# Uploads from this branch go to the experimental label, not main.
channel_targets:
  - conda-forge experimental
