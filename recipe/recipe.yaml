context:
  build_num: 4
  name: ocaml
  version: 5.3.0
  flexdll_version: 0.44
  dot: ${{ "./" if unix }}
  library: ${{ "Library/" if not unix }}
  m2: ${{ "m2-" if not unix }}
  sh: ${{ "sh" if unix else "bat" }}
  so: ${{ "so" if unix else "dll" }}
  
package:
  name: ocaml
  version: ${{ version }}

source:
  - url: https://github.com/ocaml/ocaml/archive/refs/tags/${{ version }}.tar.gz
    sha256: eb9eab2f21758d3cfb1e78c7f83f0b4dd6302824316aba4abee047a5a4f85029
  - if: win
    then:
      url: https://github.com/ocaml/flexdll/archive/refs/tags/${{ flexdll_version }}.tar.gz
      sha256: b7c6a92286f1f3065324d51083dcb16eec436a4e6e3b8df7cf836b6d7a8b9491
      target_directory: flexdll

build:
  number: ${{ build_num }}
  script:
    env:
      SKIP_MAKE_TESTS: 1
  dynamic_linking:
    missing_dso_allowlist:
      - if: linux
        then:
          - ld-linux-aarch64.${{ so }}.1  # PPC64LE cross-compiler
          - ld64.${{ so }}.2              # AARCH64 cross-compiler
      - if: osx
        then:
          - /usr/lib/libSystem.B.dylib
      - if: not unix
        then:
          - C:/Windows/system32/SHLWAPI.dll
          - C:/Windows/system32/VERSION.dll
  prefix_detection:
    force_file_type:
      text:
        # These are actual text files that need prefix relocation
        - lib/ocaml/ld.conf
        - lib/ocaml/Makefile.config
        - lib/ocaml/runtime-launch-info
        - lib/ocaml-cross-compilers/*/lib/ocaml/ld.conf
        - lib/ocaml-cross-compilers/*/lib/ocaml/Makefile.config
        - lib/ocaml-cross-compilers/*/lib/ocaml/runtime-launch-info
    ignore:
      # Only ignore bytecode files (binary format, relocation corrupts them)
      - lib/ocaml/**/*.cmo
      - lib/ocaml/**/*.cma
      - lib/ocaml/**/*.cmi
      - lib/ocaml/**/*.cmx
      - lib/ocaml/**/*.cmt
      - lib/ocaml/**/*.cmti
      # Bytecode executables - MUST ignore to avoid corrupting binary data
      # These use #!/usr/bin/env ocamlrun shebang which doesn't need relocation
      - bin/ocaml
      - bin/ocamlc.byte
      - bin/ocamlcmt
      - bin/ocamlcp
      - bin/ocamldebug
      - bin/ocamldoc
      - bin/ocamldep.byte
      - bin/ocamllex.byte
      - bin/ocamlmklib
      - bin/ocamlmktop
      - bin/ocamlobjinfo.byte
      - bin/ocamlopt.byte
      - bin/ocamloptp
      - bin/ocamlprof
      - if: (linux or osx) and x86_64
        then:
          - lib/ocaml-cross-compilers/*/lib/ocaml/**/*.cmo
          - lib/ocaml-cross-compilers/*/lib/ocaml/**/*.cma
          - lib/ocaml-cross-compilers/*/lib/ocaml/**/*.cmi
          - lib/ocaml-cross-compilers/*/lib/ocaml/**/*.cmx
          - lib/ocaml-cross-compilers/*/lib/ocaml/**/*.cmt
          - lib/ocaml-cross-compilers/*/lib/ocaml/**/*.cmti
          # Bytecode executables - MUST ignore to avoid corrupting binary data
          # These use #!/usr/bin/env ocamlrun shebang which doesn't need relocation
          - lib/ocaml-cross-compilers/*/bin/ocaml
          - lib/ocaml-cross-compilers/*/bin/ocamlc.byte
          - lib/ocaml-cross-compilers/*/bin/ocamlcmt
          - lib/ocaml-cross-compilers/*/bin/ocamlcp
          - lib/ocaml-cross-compilers/*/bin/ocamldebug
          - lib/ocaml-cross-compilers/*/bin/ocamldoc
          - lib/ocaml-cross-compilers/*/bin/ocamldep.byte
          - lib/ocaml-cross-compilers/*/bin/ocamllex.byte
          - lib/ocaml-cross-compilers/*/bin/ocamlmklib
          - lib/ocaml-cross-compilers/*/bin/ocamlmktop
          - lib/ocaml-cross-compilers/*/bin/ocamlobjinfo.byte
          - lib/ocaml-cross-compilers/*/bin/ocamlopt.byte
          - lib/ocaml-cross-compilers/*/bin/ocamloptp
          - lib/ocaml-cross-compilers/*/bin/ocamlprof

requirements:
  run_exports:
    - ${{ pin_subpackage("ocaml", upper_bound="x.x.x") }}
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - ${{ m2 }}bash >=5.2
    - ${{ m2 }}file
    - ${{ m2 }}findutils
    - ${{ m2 }}gawk
    - ${{ m2 }}grep
    - ${{ m2 }}make
    - ${{ m2 }}sed
    - ${{ m2 }}tar
    - binutils
    - if: linux and x86_64
      then:
        - ${{ c_compiler }}_impl_linux-aarch64 ${{ c_compiler_version }}.*
        - ${{ c_compiler }}_impl_linux-ppc64le ${{ c_compiler_version }}.*
        - ${{ c_stdlib }}_linux-aarch64 =${{ c_stdlib_version }}
        - ${{ c_stdlib }}_linux-ppc64le =${{ c_stdlib_version }}
        - binutils_impl_linux-aarch64
        - binutils_impl_linux-ppc64le
    - if: osx and x86_64
      then:
        - ${{ c_compiler }}_impl_osx-arm64 ${{ c_compiler_version }}.*
    - if: not unix
      then:
        - ${{ m2 }}coreutils
        - ${{ m2 }}diffutils
        - ${{ m2 }}which
    - if: aarch64 or ppc64le or arm64
      then:
        - ocaml >=5.3
        - zstd
    - if: osx
      then:
        - lld ${{ c_compiler_version }}.*
        - llvm-tools ${{ c_compiler_version }}.*
  host:
    - zstd
  run:
    # OCaml runtime links against zstd for compressed marshaling
    - zstd

tests:
  - package_contents:
      bin:
        - if: unix
          then:
            - conda-ocaml-ar
            - conda-ocaml-as
            - conda-ocaml-cc
            - conda-ocaml-ld
            - conda-ocaml-mkdll
            - conda-ocaml-mkexe
            - conda-ocaml-ranlib
          else:
            - conda-ocaml-ar.exe
            - conda-ocaml-as.exe
            - conda-ocaml-cc.exe
            - conda-ocaml-ld.exe
            - conda-ocaml-ranlib.exe
            - conda-ocaml-windres.exe
        - ocaml
        - ocamlc
        - ocamlc.byte
        - ocamlc.opt
        - ocamlcmt
        - ocamlcp
        - ocamldep
        - ocamldep.byte
        - ocamldep.opt
        - ocamllex
        - ocamllex.byte
        - ocamllex.opt
        - ocamlmklib
        - ocamlmktop
        - ocamlobjinfo
        - ocamlobjinfo.byte
        - ocamlobjinfo.opt
        - ocamlopt
        - ocamlopt.byte
        - ocamlopt.opt
        - ocamloptp
        - ocamlprof
        - ocamlrun
        - ocamlrund
        - ocamlyacc
        - if: build_platform == target_platform
          then:
            - ocamldebug
            - ocamldoc
            - ocamldoc.opt
        - if: linux and x86_64
          then:
            - aarch64-conda-linux-gnu-ocamlc
            - aarch64-conda-linux-gnu-ocamlc.opt
            - aarch64-conda-linux-gnu-ocamldep
            - aarch64-conda-linux-gnu-ocamldep.opt
            - aarch64-conda-linux-gnu-ocamlobjinfo
            - aarch64-conda-linux-gnu-ocamlobjinfo.opt
            - aarch64-conda-linux-gnu-ocamlopt
            - aarch64-conda-linux-gnu-ocamlopt.opt
            - powerpc64le-conda-linux-gnu-ocamlc
            - powerpc64le-conda-linux-gnu-ocamlc.opt
            - powerpc64le-conda-linux-gnu-ocamldep
            - powerpc64le-conda-linux-gnu-ocamldep.opt
            - powerpc64le-conda-linux-gnu-ocamlobjinfo
            - powerpc64le-conda-linux-gnu-ocamlobjinfo.opt
            - powerpc64le-conda-linux-gnu-ocamlopt
            - powerpc64le-conda-linux-gnu-ocamlopt.opt
        - if: osx and x86_64
          then:
            - arm64-apple-darwin20.0.0-ocamlc
            - arm64-apple-darwin20.0.0-ocamlc.opt
            - arm64-apple-darwin20.0.0-ocamldep
            - arm64-apple-darwin20.0.0-ocamldep.opt
            - arm64-apple-darwin20.0.0-ocamlobjinfo
            - arm64-apple-darwin20.0.0-ocamlobjinfo.opt
            - arm64-apple-darwin20.0.0-ocamlopt
            - arm64-apple-darwin20.0.0-ocamlopt.opt
        - if: unix
          then:
            - ocamlruni
          else:
            - flexlink
            - flexlink.byte
            - flexlink.opt
      files:
        - etc/conda/activate.d/ocaml_activate.${{ sh }}
        - etc/conda/deactivate.d/ocaml_deactivate.${{ sh }}
        - ${{ library }}lib/ocaml/ld.conf
        - ${{ library }}lib/ocaml/libasmrun.a
        - ${{ library }}lib/ocaml/Makefile.config
        - ${{ library }}lib/ocaml/runtime-launch-info
        - ${{ library }}lib/ocaml/*.a
        - ${{ library }}lib/ocaml/*.cma
        - ${{ library }}lib/ocaml/*.cmi
        - ${{ library }}lib/ocaml/*.cmo
        - ${{ library }}lib/ocaml/*.cmt
        - ${{ library }}lib/ocaml/*.cmti
        - ${{ library }}lib/ocaml/*.cmx
        - ${{ library }}lib/ocaml/*.cmxa
        - ${{ library }}lib/ocaml/*.ml
        - ${{ library }}lib/ocaml/*.mli
        - ${{ library }}lib/ocaml/*.o
        - ${{ library }}lib/ocaml/caml/*.h
        - ${{ library }}lib/ocaml/compiler-libs/*
        - ${{ library }}lib/ocaml/dynlink/*
        - ${{ library }}lib/ocaml/profiling/*
        - ${{ library }}lib/ocaml/str/*
        - ${{ library }}lib/ocaml/threads/*
        - ${{ library }}lib/ocaml/unix/*
        - ${{ library }}lib/ocaml/stublibs/dll*.${{ so }}
        - ${{ library }}share/doc/ocaml/*
        - if: (build_platform == target_platform) and unix
          then:
            - ${{ library }}lib/ocaml/ocamldoc/*
            - share/man/man{1,3}/*
        - if: linux and x86_64
          then:
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamlc
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamlc.opt
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamldep
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamldep.opt
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamllex
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamllex.opt
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamlmklib
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamlobjinfo
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamlobjinfo.opt
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamlopt
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamlopt.opt
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/bin/ocamlyacc
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/Makefile.config
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/libasmrun.a
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/runtime-launch-info
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/*.a
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/*.cma
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/*.cmi
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/*.cmo
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/*.cmx
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/*.cmxa
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/*.o
            - lib/ocaml-cross-compilers/aarch64-conda-linux-gnu/lib/ocaml/stublibs/dll*.${{ so }}
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamlc
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamlc.opt
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamldep
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamldep.opt
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamllex
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamllex.opt
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamlmklib
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamlobjinfo
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamlobjinfo.opt
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamlopt
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamlopt.opt
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/bin/ocamlyacc
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/lib/ocaml/Makefile.config
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/lib/ocaml/runtime-launch-info
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/lib/ocaml/*.cma
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/lib/ocaml/*.cmi
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/lib/ocaml/*.cmo
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/lib/ocaml/*.cmx
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/lib/ocaml/*.cmxa
            - lib/ocaml-cross-compilers/powerpc64le-conda-linux-gnu/lib/ocaml/stublibs/dll*.${{ so }}
        - if: osx and x86_64
          then:
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamlc
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamlc.opt
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamldep
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamldep.opt
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamllex
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamllex.opt
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamlmklib
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamlobjinfo
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamlobjinfo.opt
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamlopt
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamlopt.opt
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/bin/ocamlyacc
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/lib/ocaml/Makefile.config
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/lib/ocaml/libasmrun.a
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/lib/ocaml/runtime-launch-info
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/lib/ocaml/*.cma
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/lib/ocaml/*.cmi
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/lib/ocaml/*.cmo
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/lib/ocaml/*.cmx
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/lib/ocaml/*.cmxa
            - lib/ocaml-cross-compilers/arm64-apple-darwin20.0.0/lib/ocaml/stublibs/dll*.${{ so }}

  # Environment and resource file path validation
  - script:
      - cd testing && ${{ dot }}test-env-paths.${{ sh }}
    files:
      recipe:
        - testing/test-env-paths.${{ sh }}
    requirements:
      run:
        - binutils
        - if: not arm64
          then:
            - ${{ m2 }}grep

  # OCAML config
  - script:
      - if: unix and not arm64
        then:
          - cd testing && ./test-config.sh
        else:
          - echo ""
    files:
      recipe:
        - testing/test-config.sh
    requirements:
      run:
        - binutils_impl_${{ target_platform }}
        - if: not arm64
          then:
            - ${{ m2 }}grep

  # Cross-compiled binary architecture verification
  - script:
      - if: build_platform != target_platform
        then:
          - cd testing && ${{ dot }}test-cross-arch.${{ sh }} ${{ target_platform }}
        else:
          - echo "Native build - skipping cross-arch check"
    files:
      recipe:
        - testing/test-cross-arch.${{ sh }}
    requirements:
      run:
        - binutils

  # Tool version checks (all platforms)
  # Skip on cross-compiled platforms - QEMU emulation has limitations
  - script:
      env:
        OCAMLRUNPARAM: "s=4M,o=1000"
      content:
        - if: arm64
          then:
            - echo "Skipping tool version tests"
          else:
            - cd testing && ${{ dot }}test-tool-versions.${{ sh }} ${{ version }}
    files:
      recipe:
        - testing/test-tool-versions.${{ sh }}
    requirements:
      run:
        - ${{ m2 }}grep

  # Native-only tools (ocamldoc, ocamldebug)
  - script:
      env:
        OCAMLRUNPARAM: "s=4M,o=1000"
      content:
        - if: (build_platform == target_platform) and unix
          then: cd testing && ${{ dot }}test-native-only-tools.${{ sh }} ${{ version }}
          else: echo "Skipping native-only tools (cross-build or Windows)"
    files:
      recipe:
        - testing/test-native-only-tools.${{ sh }}
    requirements:
      run:
        - ${{ m2 }}grep

  # Compilation tests (bytecode, native, multi-file)
  # Skip on cross-compiled platforms (aarch64, ppc64le, arm64) - QEMU emulation unstable
  - script:
      env:
        OCAMLRUNPARAM: "s=4M,o=1000"
      content:
        - if: build_platform != target_platform
          then: echo "Skipping compilation tests (cross-compiled package, QEMU limitation)"
          else:
            - cd testing && ${{ dot }}test-compilation.${{ sh }} ${{ version }}
    files:
      recipe:
        - testing/test-compilation.${{ sh }}
    requirements:
      run:
        - ${{ c_compiler }}_impl_${{ target_platform }} >=${{ c_compiler_version }}
        - ${{ m2 }}file
        - ${{ m2 }}grep
        - if: osx
          then:
            - lld >=${{ c_compiler_version }}
            - llvm-tools >=${{ c_compiler_version }}

  # Cross-compiler tests (linux-64 and osx-64 only)
  - script:
      env:
        OCAMLRUNPARAM: "s=4M,o=1000"
      content:
        - if: (linux and x86_64) or (osx and x86_64)
          then:
            - cd testing && ${{ dot }}test-cross-compilers.${{ sh }} ${{ version }} ${{ build_platform }} ${{ target_platform }}
            - ${{ dot }}test-prims-architecture.${{ sh }}
          else:
            - echo "Cross-compilers only tested on linux-64 and osx-64"
    files:
      recipe:
        - testing/test-cross-compilers.${{ sh }}
        - testing/test-prims-architecture.${{ sh }}
    requirements:
      run:
        - ${{ m2 }}grep
        - if: linux and x86_64
          then:
            - ${{ c_compiler }}_impl_linux-aarch64 ${{ c_compiler_version }}.*
            - ${{ c_compiler }}_impl_linux-ppc64le ${{ c_compiler_version }}.*
            - ${{ c_stdlib }}_linux-aarch64 >=${{ c_stdlib_version }}
            - ${{ c_stdlib }}_linux-ppc64le >=${{ c_stdlib_version }}
            - binutils_impl_linux-aarch64
            - binutils_impl_linux-ppc64le
            - file
            - qemu-execve-aarch64
            - qemu-execve-ppc64le
        - if: osx and x86_64
          then:
            - ${{ c_compiler }}_impl_osx-arm64 ${{ c_compiler_version }}.*
            - lld >=${{ c_compiler_version }}
            - llvm-tools >=${{ c_compiler_version }}

  # CONDA_OCAML_* toolchain variable tests
  - script:
      env:
        OCAMLRUNPARAM: "s=4M,o=1000"
      content:
        - if: build_platform == target_platform
          then: cd testing && ${{ dot }}test-toolchain-vars.${{ sh }}
          else: echo "Toolchain var tests only on native builds"
    files:
      recipe:
        - testing/test-toolchain-vars.${{ sh }}
    requirements:
      run:
        - ${{ c_compiler }}_impl_${{ target_platform }} >=${{ c_compiler_version }}
        - ${{ m2 }}grep
        - if: osx
          then:
            - lld >=${{ c_compiler_version }}
            - llvm-tools >=${{ c_compiler_version }}

about:
  homepage: https://ocaml.org/
  license: LGPL-2.1-only WITH OCaml-LGPL-linking-exception
  license_family: LGPL
  license_file: LICENSE
  summary: Objective Caml (OCaml) is an implementation of the ML language.
  description: |
    OCaml is an implementation of the ML language, based on the Caml Light
    dialect extended with a complete class-based object system and a powerful
    module system in the style of Standard ML.

    OCaml comprises two compilers. One generates bytecode which is then
    interpreted by a C program. This compiler runs quickly, generates compact
    code with moderate memory requirements, and is portable to essentially any
    32 or 64 bit Unix platform. Performance of generated programs is quite good
    for a bytecoded implementation.  This compiler can be used either as a
    standalone, batch-oriented compiler that produces standalone programs, or as
    an interactive, toplevel-based system.

    The other compiler generates high-performance native code for a number of
    processors. Compilation takes longer and generates bigger code, but the
    generated programs deliver excellent performance, while retaining the
    moderate memory requirements of the bytecode compiler.
  documentation: https://ocaml.org/docs/
  repository: https://github.com/ocaml/ocaml

extra:
  recipe-maintainers:
    - isuruf
    - dslarm
    - MementoRC
